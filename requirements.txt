# Core dependencies
sqlalchemy>=2.0.0
jinja2>=3.0.0
requests>=2.25.0
tiktoken>=0.5.0

# Database drivers (optional - install based on your database choice)
# For PostgreSQL (recommended for cloud):
#   pip install psycopg2-binary
# For MySQL/MariaDB:
#   pip install pymysql
# SQLite support is built-in (no extra packages needed)

# For HTML/report generation
markdown>=3.3.0

# For JSON processing
simplejson>=3.17.0

# Testing
pytest>=7.0.0

# Chinese
pypinyin>=0.53.0
jieba>=0.42.1
